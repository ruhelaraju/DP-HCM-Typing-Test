if os.path.exists("admin_credentials.txt"):
    with open("admin_credentials.txt", "r") as f:
        credentials = f.read().strip().split(",")

    saved_username = credentials[0]
    saved_password = credentials[1]

    if username == saved_username and password == saved_password:
        st.session_state.page = "admin_panel"
        st.rerun()
    else:
        st.error("Invalid Credentials")
else:
    st.error("Admin credentials file not found.")
